@{
    ViewData["Title"] = "OSM";
}

<h1>OSM</h1>

@using (Html.BeginForm(method: FormMethod.Post))
{
    @Html.TextBox("coords")
    <br />
    <input type="submit" value="Выбрать" class="btn btn-primary" />
}