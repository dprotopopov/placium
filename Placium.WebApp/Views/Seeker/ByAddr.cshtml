@{
    ViewData["Title"] = "Seeker";
}

<h1>Seeker</h1>

@using (Html.BeginForm(method: FormMethod.Post))
{
    <label>Элементы адреса (через запятую)</label>
    @Html.TextBox("addr")
    <br />
    <label>Номер дома/участка</label>
    @Html.TextBox("housenumber")
    <br />
    <input type="submit" value="Выбрать" class="btn btn-primary" />
}